# Context

本目录只在证据不足时使用，不是默认中转站。

## 什么时候才需要读 `context/`

- 用户输入不足以定位目标
- 用户输入不足以判断边界、规则或代码入口
- `docs/index.md` 指向的目录仍不足以支持判断

## 进入后怎么读

- 按需读取，不默认全读
- `project.md`：当前项目状态、关注点、已知风险
- `rules.md`：详细操作规则、边界、Ask First 条件
- `map.md`：代码入口、常见起点、禁扫区
