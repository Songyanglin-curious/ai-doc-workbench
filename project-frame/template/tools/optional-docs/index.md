# Optional Docs

本目录提供按需扩展模板。

默认情况下，不把这些目录复制到 `docs/` 主骨架。

## 当前可选目录

- `audits/`
- `testing/`
- `bugs/`
- `analysis/`
- `retrospectives/`

## 使用规则

- 当项目复杂度或风险证明需要时，再复制到 `docs/`
- 复制后，仍遵守“每个目录必须有 `index.md`”的规则
